{
  "name": "@adaptive-agent/gateway-fastify",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir dist --target bun",
    "logs:analyze": "bun run ./src/analyze-logs.ts",
    "trace-session": "bun run ./src/trace-session.ts",
    "client:local": "bun run ./src/local-ws-client.ts",
    "client:tui": "bun run ./src/local-ws-client-tui.ts",
    "mint:local-jwt": "bun run ./src/mint-local-jwt.ts",
    "start:local": "bun run ./src/start-local.ts",
    "test": "bunx vitest run",
    "test:postgres-integration": "RUN_DOCKER_INTEGRATION_TESTS=1 bunx vitest run --testTimeout 300000 --hookTimeout 300000 src/postgres-restart.integration.test.ts",
    "test:watch": "bunx vitest",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@adaptive-agent/core": "workspace:*",
    "@fastify/websocket": "^11.2.0",
    "@mariozechner/pi-tui": "^0.66.1",
    "chalk": "^5.5.0",
    "fastify": "^5.6.1",
    "jose": "^6.2.2",
    "marked": "^17.0.5",
    "marked-terminal": "^7.3.0",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "@types/pg": "^8.20.0",
    "bun-types": "^1.3.5",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  }
}
