## Codebase Patterns
- Keep `session` as a gateway-owned host artifact; do not add it to `@adaptive-agent/core` contracts.
- Preserve the v1.4 boundary: `run` is the execution unit, `rootRunId` identifies a run tree, and delegation remains synthetic `delegate.*` tools plus child runs.
- Prefer Bun + Fastify in `packages/gateway-fastify`, with explicit JSON-friendly config and protocol types.
- In `packages/gateway-fastify`, route TypeScript imports through `src/core.ts` so the package can typecheck against the built core runtime without inheriting unrelated `packages/core/src` errors.
- When `packages/gateway-fastify` needs runtime access to `../../core/dist/index.js`, load it lazily inside `src/core.ts`; eager imports make Vitest execute Bun-specific bundle helpers before tests can substitute their own factories.
- Keep WebSocket protocol routing in a pure helper such as `handleGatewaySocketMessage(...)` so protocol behavior can be tested without depending on Bun/WebSocket client teardown semantics.
- Keep routing deterministic and host-owned; bindings and session pinning decide `agentId`, not model output.
- Evaluate binding specificity in `packages/gateway-fastify` as channel, then tenant, then roles, and break equal-specificity ties by config order so routing stays deterministic and auditable.
- Keep the rolling transcript summary on the gateway session record and derive replay envelopes from `session.transcriptSummary` plus the recent raw transcript window; do not move chat history ownership into the core runtime.
- For `message.send`, build the replay envelope in memory first and only persist transcript records after the chat result is known so failed turns do not corrupt prior session history.
- Gateway stores own sessions, transcripts, session-run links, and cron metadata; runtime stores remain authoritative for execution facts.
- Per session, enforce a single write lane and a single active root run while allowing multiple same-principal observers.
- Reuse one orchestrator for WebSocket traffic and cron-triggered work so chat, run, and approval behavior stay aligned.
- Channel subscriptions use `scope:id` format; parse with `parseChannelId(...)` and manage with `createChannelSubscriptionManager()`.
- Redact outbound event data with `redactSensitiveMetadata(...)` and project auth contexts with `redactAuthClaims(...)` before delivering to clients.
- Execute gateway hooks via `executeHookSlot(hooks, slot, context)` which handles ordering, rejection, enrichment, and failure policy consistently.
- File-backed stores use `createFileGatewayStores({ baseDir })` for local dev; Postgres stores use `createPostgresSessionStores({ client })` and `createPostgresCronStores({ client })` for production.
- The `PostgresClient` interface is a minimal `query<T>(sql, params)` contract; any pg-compatible driver works.
- Lease-based cron claiming uses atomic conditional UPDATEs; `claimLease` returns `undefined` when another worker holds the lease.


## 2026-04-08 16:01 IST - US-001
Thread: https://ampcode.com/threads/T-019d6c97-de3e-720f-918f-a2391c84675e
- Implemented the new `packages/gateway-fastify` workspace with Bun `build`, `test`, and `typecheck` scripts, a package entrypoint, a Fastify WebSocket bootstrap stub, and a higher-level gateway bootstrap helper.
- Files changed: `packages/gateway-fastify/package.json`, `packages/gateway-fastify/tsconfig.json`, `packages/gateway-fastify/src/index.ts`, `packages/gateway-fastify/src/server.ts`, `packages/gateway-fastify/src/bootstrap.ts`, `packages/gateway-fastify/src/core.ts`, `packages/gateway-fastify/src/core-runtime.d.ts`, `bun.lock`.
- **Learnings for future iterations:**
  - The gateway package can already load config, agents, and registries before the protocol layer exists, so later stories can wire behavior into `bootstrapGateway(...)` instead of adding a second startup path.
  - The initial WebSocket route intentionally closes connections with a stable stub until protocol validation lands.
---

## 2026-04-08 16:01 IST - US-002
Thread: https://ampcode.com/threads/T-019d6c97-de3e-720f-918f-a2391c84675e
- Added typed gateway and agent config loaders with deterministic validation for server, auth, channels, bindings, hooks, invocation modes, model config, defaults, routing, and metadata.
- Files changed: `packages/gateway-fastify/src/config.ts`, `packages/gateway-fastify/src/config.test.ts`.
- **Learnings for future iterations:**
  - Gateway auth config is normalized into `{ provider, settings }`, which preserves JSON-friendly provider-specific fields without allowing inline executable behavior.
  - Hook slots default to empty arrays when omitted, so config can stay terse while validation remains explicit.
---

## 2026-04-08 16:01 IST - US-003
Thread: https://ampcode.com/threads/T-019d6c97-de3e-720f-918f-a2391c84675e
- Added explicit registries for tools, delegates, hooks, and auth providers, along with fast-fail resolution helpers for gateway and agent config references.
- Files changed: `packages/gateway-fastify/src/registries.ts`, `packages/gateway-fastify/src/registries.test.ts`.
- **Learnings for future iterations:**
  - Unknown registry references now fail with the source label and the currently registered names, which makes startup errors much easier to act on.
  - Hook and auth references stay declarative strings resolved by code-owned registries rather than file paths or inline code in JSON.
---

## 2026-04-08 16:01 IST - US-004
Thread: https://ampcode.com/threads/T-019d6c97-de3e-720f-918f-a2391c84675e
- Added a lazy `AgentRegistry` that resolves validated configs up front, exposes invocation metadata synchronously, and materializes cached `AdaptiveAgent` bundles only on first use.
- Files changed: `packages/gateway-fastify/src/agent-registry.ts`, `packages/gateway-fastify/src/agent-registry.test.ts`, `packages/gateway-fastify/src/bootstrap.ts`.
- **Learnings for future iterations:**
  - The registry caches the in-flight materialization promise, so concurrent first-use lookups for the same `agentId` collapse onto a single factory call.
  - Routing-level checks for `defaultAgentId` and binding `agentId` references are already centralized in `bootstrapGateway(...)`, which is the right place to extend startup validation later.
---

## 2026-04-08 16:14 IST - US-005
Thread: https://ampcode.com/threads/T-019d6c97-de3e-720f-918f-a2391c84675e
- Added a concrete WebSocket protocol contract with inbound and outbound frame types, stable protocol error codes, runtime parsing/validation, and helpers for `pong` and error responses.
- Files changed: `packages/gateway-fastify/src/protocol.ts`, `packages/gateway-fastify/src/protocol.test.ts`, `packages/gateway-fastify/src/index.ts`.
- **Learnings for future iterations:**
  - The protocol layer now distinguishes `invalid_json`, `invalid_frame`, `unknown_frame_type`, and `unsupported_frame`, which gives later session/auth work a stable error surface to build on.
  - `run.start` already accepts `goal`, optional `input`, optional `context`, and optional `metadata`, so future orchestration work can plug into the current frame shape instead of redefining it.
---

## 2026-04-08 16:14 IST - US-006
Thread: https://ampcode.com/threads/T-019d6c97-de3e-720f-918f-a2391c84675e
- Wired the Fastify WebSocket route through the validated protocol handler, added a minimal health response that exposes the configured WebSocket path, and added a real listen smoke test for server startup.
- Files changed: `packages/gateway-fastify/src/server.ts`, `packages/gateway-fastify/src/server.test.ts`.
- **Learnings for future iterations:**
  - The server now routes every socket message through `handleGatewaySocketMessage(...)`, so later features can extend one shared ingress path instead of scattering frame logic across route handlers.
  - Under Bun, pure protocol-handler tests are more reliable than full live WebSocket teardown tests for iteration speed, while a simple `listen()` smoke test still covers server bootstrap.
---

## 2026-04-08 16:30 IST - US-007
Thread: https://ampcode.com/threads/T-019d6cb7-6510-71ae-88d2-c0982d274fb2
- Implemented JWT-backed upgrade authentication with a built-in `jwt` auth provider, normalized auth contexts, public-channel bypass via upgrade `channelId`, and stable `auth_required`, `invalid_token`, and `token_expired` error codes.
- Files changed: `packages/gateway-fastify/package.json`, `bun.lock`, `packages/gateway-fastify/src/auth.ts`, `packages/gateway-fastify/src/protocol.ts`, `packages/gateway-fastify/src/registries.ts`, `packages/gateway-fastify/src/bootstrap.ts`, `packages/gateway-fastify/src/server.ts`, `packages/gateway-fastify/src/index.ts`, `packages/gateway-fastify/src/auth.test.ts`, `packages/gateway-fastify/src/registries.test.ts`.
- **Learnings for future iterations:**
  - The gateway can resolve a built-in JWT provider through the same auth registry mechanism used for custom providers, so future auth hardening can extend the registry rather than bypassing it.
  - Under Bun, `@fastify/websocket` route behavior is easier to verify by testing `authenticateGatewayUpgrade(...)` directly and keeping the server suite focused on HTTP/listen smoke coverage.
---

## 2026-04-08 16:30 IST - US-008
Thread: https://ampcode.com/threads/T-019d6cb7-6510-71ae-88d2-c0982d274fb2
- Added gateway-owned store interfaces and in-memory implementations for sessions, transcript messages, session-run links, cron jobs, and cron runs, plus regression tests that cover ordering, lookup, due-job filtering, and clone isolation.
- Files changed: `packages/gateway-fastify/src/stores.ts`, `packages/gateway-fastify/src/stores.test.ts`, `packages/gateway-fastify/src/index.ts`.
- **Learnings for future iterations:**
  - The in-memory stores already model the core ownership split the gateway needs later: session and cron facts stay in gateway records while run execution details remain linked by IDs.
  - Returning cloned records from store reads and writes protects tests and future orchestrator code from mutating authoritative in-memory state by accident.
---

## 2026-04-08 17:34 IST - US-009
Thread: https://ampcode.com/threads/T-019d6cf4-6f04-73a2-85a6-ae341f37db42
- Implemented authenticated `session.open` handling so the gateway now creates session records, reattaches same-principal reconnects, rejects cross-principal attachment attempts, and threads a shared gateway store instance through bootstrap/server startup.
- Files changed: `packages/gateway-fastify/src/session.ts`, `packages/gateway-fastify/src/server.ts`, `packages/gateway-fastify/src/server.test.ts`, `packages/gateway-fastify/src/protocol.ts`, `packages/gateway-fastify/src/protocol.test.ts`, `packages/gateway-fastify/src/bootstrap.ts`, `packages/gateway-fastify/src/index.ts`, `packages/gateway-fastify/src/core.ts`, `tasks/prd.json`, `tasks/progress.txt`.
- **Learnings for future iterations:**
  - The session opener is now a dedicated helper that takes `authContext`, `stores`, `now`, and `sessionIdFactory`, which makes session lifecycle behavior deterministic in tests and reusable for later message/run orchestration.
  - Sharing one `GatewayStores` instance across `bootstrapGateway(...)` and `createGatewayServer(...)` keeps reconnects and multi-connection observers attached to the same authoritative session state.
  - Lazy-loading the built core runtime in `src/core.ts` keeps gateway tests from evaluating Bun-specific bundle helpers unless the default agent factory is actually used.
---

## 2026-04-09 06:10 IST - US-010
Thread: https://ampcode.com/threads/T-019d6fa7-b7b4-71ec-ac7f-decdeb062504
- Added a deterministic routing helper that resolves session pins before bindings, ranks bindings by channel/tenant/roles specificity, falls back to the gateway default agent, and rejects direct `agentId` overrides for normal chat traffic.
- Files changed: `packages/gateway-fastify/src/routing.ts`, `packages/gateway-fastify/src/routing.test.ts`, `packages/gateway-fastify/src/protocol.ts`, `packages/gateway-fastify/src/index.ts`, `tasks/prd.json`, `tasks/progress.txt`.
- **Learnings for future iterations:**
  - Keep routing as a pure function over session state, auth context, gateway config, and agent metadata so later chat/run orchestrators can reuse one auditable decision point.
  - Binding precedence is now explicit and test-backed: session pin first, then the most specific binding by channel/tenant/roles, then `defaultAgentId`.
  - Session invocation mode should be treated as part of the routing contract; once a session is pinned to `chat` or `run`, later handlers should reject mismatched traffic instead of silently rebinding.
---

## 2026-04-09 06:13 IST - US-011
Thread: https://ampcode.com/threads/T-019d6fa7-b7b4-71ec-ac7f-decdeb062504
- Added gateway transcript policy config, a persisted `transcriptSummary` field on sessions, and pure helpers that build rolling summaries plus replay envelopes from the recent raw transcript window.
- Files changed: `packages/gateway-fastify/src/config.ts`, `packages/gateway-fastify/src/config.test.ts`, `packages/gateway-fastify/src/stores.ts`, `packages/gateway-fastify/src/core.ts`, `packages/gateway-fastify/src/transcript.ts`, `packages/gateway-fastify/src/transcript.test.ts`, `packages/gateway-fastify/src/index.ts`, `tasks/prd.json`, `tasks/progress.txt`.
- **Learnings for future iterations:**
  - Transcript replay is now gateway-owned and deterministic: `buildTranscriptReplayEnvelope(...)` consumes the session summary plus the recent stored messages without needing runtime snapshots.
  - The transcript policy defaults are intentionally conservative and can be overridden in gateway config, which lets later stories tune replay size without rewriting orchestrator logic.
  - `src/core.ts` now mirrors the `chat()`/run result shapes the gateway needs, so later orchestration code can stay type-safe without importing `packages/core/src` directly.
---

## 2026-04-09 06:16 IST - US-012
Thread: https://ampcode.com/threads/T-019d6fa7-b7b4-71ec-ac7f-decdeb062504
- Wired `message.send` through authorized session lookup, deterministic routing, transcript replay, `agent.chat(...)`, session-run linkage persistence, and stable success/failure protocol frames.
- Files changed: `packages/gateway-fastify/src/chat.ts`, `packages/gateway-fastify/src/server.ts`, `packages/gateway-fastify/src/session.ts`, `packages/gateway-fastify/src/server.test.ts`, `packages/gateway-fastify/src/stores.test.ts`, `packages/gateway-fastify/src/bootstrap.ts`, `packages/gateway-fastify/src/index.ts`, `packages/gateway-fastify/src/protocol.ts`, `tasks/prd.json`, `tasks/progress.txt`.
- **Learnings for future iterations:**
  - `getAuthorizedGatewaySession(...)` is now the shared ownership check for post-open session operations, so later run/approval handlers should reuse it instead of reimplementing auth checks.
  - The chat orchestrator resolves `rootRunId` from the runtime run store after `agent.chat(...)`, which keeps gateway linkage authoritative without assuming `runId === rootRunId` forever.
  - Approval-requested chat turns already persist the user message and leave the session in `awaiting_approval`, which gives later approval/resume work a stable starting point.
---

## 2026-04-09 07:57 IST - US-013
Thread: https://ampcode.com/threads/T-019d700a-a7d9-7318-9eb6-9a2bd8444606
- Implemented `run.start` orchestration for both session-bound and isolated structured runs, including run-mode routing, channel-mode validation, `agent.run(...)` invocation, root-run lookup, session-run linkage persistence, and `run.output` responses.
- Files changed: `packages/gateway-fastify/src/run.ts`, `packages/gateway-fastify/src/server.ts`, `packages/gateway-fastify/src/server.test.ts`, `packages/gateway-fastify/src/index.ts`, `tasks/prd.json`, `tasks/progress.txt`.
- **Learnings for future iterations:**
  - Sessionless structured runs should still carry the upgrade `channelId` into routing and run context so endpoint policy stays deterministic without fabricating gateway session state.
  - Structured run handlers should resolve `rootRunId` from the runtime store for both success and failure paths before publishing gateway output or mutating session linkage.
  - Session-bound `run.start` pins the session to invocation mode `run`, which gives later approval and concurrency checks one consistent source of truth.
---

## 2026-04-09 08:00 IST - US-014
Thread: https://ampcode.com/threads/T-019d700a-a7d9-7318-9eb6-9a2bd8444606
- Implemented `approval.resolve` handling that validates the authorized pending session run, calls `resolveApproval(...)` and `resume(...)` on the same run handle, and maps the resumed result back onto gateway session state and `run.output` frames.
- Files changed: `packages/gateway-fastify/src/session.ts`, `packages/gateway-fastify/src/run.ts`, `packages/gateway-fastify/src/server.ts`, `packages/gateway-fastify/src/server.test.ts`, `tasks/prd.json`, `tasks/progress.txt`.
- **Learnings for future iterations:**
  - Approval frames should key off the session's `currentRunId` rather than trusting a client-supplied `runId`; that keeps resume authorization host-owned and deterministic.
  - Approval resume should reuse the original session-run link instead of appending a second link, because the resumed work is still part of the same root run.
  - Moving the session back to `running` before `resume(...)` keeps the gateway's write lane aligned with the runtime while the paused run is being continued.
---

## 2026-04-09 08:02 IST - US-015
Thread: https://ampcode.com/threads/T-019d700a-a7d9-7318-9eb6-9a2bd8444606
- Added a shared session write-lane guard so `message.send`, `run.start`, and `approval.resolve` now enforce a single active root run, reject non-approval writes while a session is `awaiting_approval`, and still allow same-principal observers to reattach.
- Files changed: `packages/gateway-fastify/src/protocol.ts`, `packages/gateway-fastify/src/session.ts`, `packages/gateway-fastify/src/chat.ts`, `packages/gateway-fastify/src/run.ts`, `packages/gateway-fastify/src/server.test.ts`, `tasks/prd.json`, `tasks/progress.txt`.
- **Learnings for future iterations:**
  - Keep session-state concurrency checks centralized in `src/session.ts`; handler-specific status guards drift quickly once chat, run, and approval paths evolve independently.
  - Use protocol-level error codes such as `session_busy` and `approval_required` for session-state contention so multi-client behavior stays stable and testable.
  - Session reattachment should remain read-safe even while a run is active, so concurrency enforcement belongs on write frames rather than on `session.open`.
---

## 2026-04-09 10:01 IST - US-016
Thread: https://ampcode.com/threads/T-019d707b-b23c-7138-9830-a0838054d9e2
- Added channel subscription manager with session, run, root-run, and agent scopes; runtime event bridge with full correlation metadata; and `channel.subscribe` handling in the server handler.
- Files changed: `packages/gateway-fastify/src/channels.ts`, `packages/gateway-fastify/src/channels.test.ts`, `packages/gateway-fastify/src/server.ts`, `packages/gateway-fastify/src/server.test.ts`, `packages/gateway-fastify/src/index.ts`.
- **Learnings for future iterations:**
  - Channel ids use a `scope:id` format (`session:s-1`, `run:r-1`, `root-run:root-1`, `agent:support-agent`) parsed and validated at subscription time.
  - The subscription manager is a per-connection in-memory structure; later persistence or cross-process fanout can be layered on top without changing the subscription API.
  - The 10 bridged runtime event types cover the full run lifecycle and delegation tree; custom/unknown events are not bridged by default.
---

## 2026-04-09 10:01 IST - US-017
Thread: https://ampcode.com/threads/T-019d707b-b23c-7138-9830-a0838054d9e2
- Added outbound authorization for channel subscriptions (session ownership check), sensitive metadata redaction (JWT claims and credential keys), and auth claim projection for safe outbound contexts.
- Files changed: `packages/gateway-fastify/src/outbound.ts`, `packages/gateway-fastify/src/outbound.test.ts`, `packages/gateway-fastify/src/index.ts`.
- **Learnings for future iterations:**
  - Session-scoped subscriptions enforce principal ownership via the session store; non-session scopes (run, root-run, agent) pass through for any authenticated caller.
  - Redaction uses a static set of known sensitive keys (`token`, `apiKey`, `password`, `secret`, JWT standard claims) and recurses into nested objects and arrays.
  - `redactAuthClaims(...)` projects only `subject`, `tenantId`, and `roles` from the full auth context, which prevents accidental claim leakage in outbound frames.
---

## 2026-04-09 10:01 IST - US-018
Thread: https://ampcode.com/threads/T-019d707b-b23c-7138-9830-a0838054d9e2
- Added a gateway hook lifecycle with `executeHookSlot(...)` that runs all registered hooks for a given slot in order, supports before-hook rejection and metadata enrichment, and respects the configured failure policy (`fail`, `warn`, `ignore`).
- Files changed: `packages/gateway-fastify/src/hooks.ts`, `packages/gateway-fastify/src/hooks.test.ts`, `packages/gateway-fastify/src/index.ts`.
- **Learnings for future iterations:**
  - Before hooks can reject by returning `{ rejected: true, rejectionReason }` or enrich by returning `{ metadata }`, and enriched metadata flows forward to subsequent hooks in the same slot.
  - `HookExecutionError` captures `hookId` and `slot` so callers can map failures to specific protocol error codes.
  - `createEmptyResolvedHooks(...)` is the canonical way to get a hook structure with all slots initialized to empty arrays, useful for tests and default construction.
---

## 2026-04-09 10:11 IST - US-019
Thread: https://ampcode.com/threads/T-019d707b-b23c-7138-9830-a0838054d9e2
- Added file-backed stores for all 5 gateway store interfaces with a documented on-disk layout: sessions, transcripts (per-session subdirs), session-run-links, cron-jobs, and cron-runs.
- Files changed: `packages/gateway-fastify/src/stores-file.ts`, `packages/gateway-fastify/src/stores-file.test.ts`, `packages/gateway-fastify/src/index.ts`.
- **Learnings for future iterations:**
  - The on-disk layout uses `<baseDir>/<collection>/<id>.json` for flat stores and `<baseDir>/transcripts/<sessionId>/<messageId>.json` for session-scoped transcript messages.
  - File stores read all records on every list/filter call, which is fine for local dev but not for production; Postgres stores are the production path.
  - `createFileGatewayStores({ baseDir })` is the canonical factory; reloading from the same `baseDir` after restart recovers all state.
---

## 2026-04-09 10:14 IST - US-020
Thread: https://ampcode.com/threads/T-019d707b-b23c-7138-9830-a0838054d9e2
- Added PostgreSQL-backed stores for sessions, transcript messages, and session-run links with documented schema, typed SQL queries, and bidirectional row↔record mapping.
- Files changed: `packages/gateway-fastify/src/stores-postgres.ts`, `packages/gateway-fastify/src/stores-postgres.test.ts`, `packages/gateway-fastify/src/index.ts`.
- **Learnings for future iterations:**
  - The `PostgresClient` interface (`query<T>(sql, params) => { rows, rowCount }`) is intentionally minimal so any pg-compatible driver can be plugged in.
  - All SQL is exposed as named query constants (`POSTGRES_SESSION_QUERIES`, etc.) so schema migrations and query audits can reference stable string keys.
  - Gateway tables (`gateway_sessions`, `gateway_transcript_messages`, `gateway_session_run_links`) are namespaced to avoid collisions with runtime execution tables.
---

## 2026-04-09 10:16 IST - US-021
Thread: https://ampcode.com/threads/T-019d707b-b23c-7138-9830-a0838054d9e2
- Added PostgreSQL-backed cron job and cron run stores with lease-based claiming via atomic `claimLease(...)` and `releaseLease(...)` SQL operations.
- Files changed: `packages/gateway-fastify/src/stores-postgres-cron.ts`, `packages/gateway-fastify/src/stores-postgres-cron.test.ts`, `packages/gateway-fastify/src/index.ts`.
- **Learnings for future iterations:**
  - `claimLease` uses a conditional UPDATE (`WHERE lease_owner IS NULL OR lease_expires_at < now`) so only one worker can claim a due fire time; returns `undefined` if the lease is already held.
  - `releaseLease` requires the caller to prove ownership via `lease_owner` match, preventing accidental release of another worker's lease.
  - Cron stores extend the base `CronJobStore`/`CronRunStore` interfaces with lease-aware methods, so the scheduler can use them while generic code keeps the simpler interface.
---

## 2026-04-09 10:35 IST - US-022
Thread: https://ampcode.com/threads/T-019d709a-84c3-748d-9fb0-778b0930f20e
- Added a scheduler loop with `createSchedulerLoop(...)` that polls for due cron jobs, deduplicates by fire time, creates cron run records, and dispatches through `executeCronTarget(...)`.
- Implemented support for all 3 cron target kinds: `session_event` (reuses existing session via `executeGatewayChatTurn`), `isolated_run` (calls `executeGatewayRunStart` without session state), and `isolated_chat` (creates a fresh session then chats).
- Approval-requested cron results are mapped to `needs_review` status instead of blocking indefinitely.
- Files changed: `packages/gateway-fastify/src/scheduler.ts`, `packages/gateway-fastify/src/scheduler.test.ts`, `packages/gateway-fastify/src/index.ts`.
- **Learnings for future iterations:**
  - Cron dispatch reuses the same chat/run orchestrators as live WebSocket traffic, so behavior stays aligned without duplicating routing, transcript, or session-state logic.
  - The scheduler loop deduplicates via `findByFireTime(...)` before dispatch, which prevents double execution even without lease-based claiming at the in-memory store level.
  - `buildCronAuthContext(...)` synthesizes a gateway auth context from the job target, so cron work runs under an explicit principal without requiring a real JWT.
---

## 2026-04-09 10:35 IST - US-023
Thread: https://ampcode.com/threads/T-019d709a-84c3-748d-9fb0-778b0930f20e
- Added `deliverCronResult(...)` with support for delivery modes: `session` (appends system transcript message), `announce` (channel-based, stubbed for later fanout), `webhook` (HTTP POST with completion payload), and `none`.
- Webhook delivery includes `X-Webhook-Secret` header when configured and reports HTTP/fetch errors.
- Added `resolveCronRunStatusForApproval(...)` to map approval-blocked cron runs to `needs_review` or `failed` based on policy.
- Files changed: `packages/gateway-fastify/src/cron-delivery.ts`, `packages/gateway-fastify/src/cron-delivery.test.ts`, `packages/gateway-fastify/src/index.ts`.
- **Learnings for future iterations:**
  - Webhook delivery posts a `cron.completed` payload with jobId, cronRunId, status, and run linkage; the shape is stable for external consumers.
  - Session delivery appends a `system` role transcript message so cron results appear naturally in the session chat history.
  - Announce delivery is intentionally minimal; full channel fanout can be wired in later through the subscription manager.
---

## 2026-04-09 10:35 IST - US-024
Thread: https://ampcode.com/threads/T-019d709a-84c3-748d-9fb0-778b0930f20e
- Added `restoreActiveSession(...)` that recovers session state on reconnect, including `session.opened` and `session.updated` frames, pending approval state, and authorized channel subscriptions.
- Reconnecting sockets receive current status, active run linkage, and are auto-subscribed to session, run, root-run, and agent channels.
- Pending approval sessions surface their outstanding runId and rootRunId after reconnect.
- Files changed: `packages/gateway-fastify/src/reconnect.ts`, `packages/gateway-fastify/src/reconnect.test.ts`, `packages/gateway-fastify/src/index.ts`.
- **Learnings for future iterations:**
  - Reconnect auto-subscribes the client to the relevant channels based on active session state, so observers immediately see in-flight run events without an extra `channel.subscribe` frame.
  - The reconnect helper does not duplicate channels when `currentRunId === currentRootRunId`.
  - Reconnect reuses the same ownership checks as `getAuthorizedGatewaySession(...)` — different principals are always rejected.
---

## 2026-04-09 10:42 IST - US-025
Thread: https://ampcode.com/threads/T-019d709a-84c3-748d-9fb0-778b0930f20e
- Added `createGatewayHealthManager(...)` with state transitions for healthy, startup_failed, and degraded, tracking stores availability, scheduler status, and agent count.
- Added `createGatewayMetrics()` with increment/decrement/reset for 12 operational counters (sessions, runs, auth failures, routing misses, cron claims, etc.).
- Added `createGatewayLogger(sink?)` emitting structured JSON log entries with level, event, message, timestamp, and data.
- Defined `GATEWAY_LOG_EVENTS` covering all operational categories (auth, sessions, routing, chat, runs, hooks, cron, protocol).
- Files changed: `packages/gateway-fastify/src/observability.ts`, `packages/gateway-fastify/src/observability.test.ts`, `packages/gateway-fastify/src/index.ts`.
- **Learnings for future iterations:**
  - Health state transitions are one-directional in severity: `healthy` → `degraded` → `startup_failed`; callers use `setState(...)` for explicit overrides.
  - Metrics counters return snapshots via the `counters` getter, not mutable references.
  - The default log sink writes JSON to `console.log`/`console.warn`/`console.error`; custom sinks can redirect to external logging systems.
---

## 2026-04-09 10:42 IST - US-026
Thread: https://ampcode.com/threads/T-019d709a-84c3-748d-9fb0-778b0930f20e
- Created comprehensive `README.md` documenting gateway config structure, agent config structure, all inbound and outbound WebSocket frames, routing behavior, session concurrency, scheduler target kinds and delivery modes, reconnect expectations, hooks, health/observability, and project structure.
- Added example JSON config files in `packages/gateway-fastify/examples/` for gateway and agent configuration.
- Files changed: `packages/gateway-fastify/README.md`, `packages/gateway-fastify/examples/gateway.json`, `packages/gateway-fastify/examples/agent.json`.
- **Learnings for future iterations:**
  - The README serves as the canonical integration guide; keep it in sync with protocol.ts frame types and config.ts schema when either evolves.
  - Example configs use safe defaults (cron disabled, warn hook policy, in-memory stores implied) so developers can start locally without external dependencies.
---
